# Dev notes

Topic-grouped notes captured while solving real problems on CW19_Test.

| File | Topic |
|---|---|
| [qweb-inherit-gotchas.md](qweb-inherit-gotchas.md) | Pitfalls writing template inherits over stock Odoo reports (signature crash, t-if/t-else chain detachment, variable names, `ref()` scope). |

When you add a file, add a one-line entry to the table above. Files are
topic-grouped, not date-grouped — keep adding to existing files when a new
gotcha fits an existing topic.
