<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="module_category_cw_shipping" model="ir.module.category">
        <field name="name">Shipping</field>
        <field name="description">Courtwell Shipping Department</field>
        <field name="sequence">25</field>
    </record>

    <record id="privilege_cw_shipping" model="res.groups.privilege">
        <field name="name">Shipping</field>
        <field name="category_id" ref="module_category_cw_shipping"/>
        <field name="sequence">10</field>
    </record>

    <record id="group_shipping_user" model="res.groups">
        <field name="name">Shipping / User</field>
        <field name="privilege_id" ref="privilege_cw_shipping"/>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        <field name="comment">Shipping Department user. Sees CRM opportunities from Proforma Invoice (stage sequence 3) onwards and owns the cw.shipment record per opportunity. IMPORTANT: members of this group must NOT also be in sales_team.group_sale_salesman_all_leads ("User: All Documents"), otherwise that group's broad read access OR-overrides this module's stage-gated record rules.</field>
    </record>

</odoo>
